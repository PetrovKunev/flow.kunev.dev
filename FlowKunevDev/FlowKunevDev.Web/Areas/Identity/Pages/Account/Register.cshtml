@page
@model RegisterModel
@{
    ViewData["Title"] = "Регистрация";
}

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card border-success">
            <div class="card-header text-center">
                <h1 class="card-title mb-0">@ViewData["Title"]</h1>
            </div>
            <div class="card-body">
                <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                    <h4 class="text-center mb-4">Създайте нов акаунт</h4>
                    <hr />
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="име@пример.com" />
                        <label asp-for="Input.Email">Електронна поща</label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Password" class="form-control" autocomplete="new-password" aria-required="true" placeholder="парола" />
                        <label asp-for="Input.Password">Парола</label>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="потвърдете паролата" />
                        <label asp-for="Input.ConfirmPassword">Потвърдете паролата</label>
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                    </div>

                    <div class="d-grid">
                        <button id="registerSubmit" type="submit" class="btn btn-lg btn-success">Регистрирай се</button>
                    </div>

                    <div class="text-center mt-3">
                        <p class="mb-0">
                            Вече имате акаунт? <a asp-area="Identity" asp-page="/Account/Login" asp-route-returnUrl="@Model.ReturnUrl" class="text-decoration-none">Влезте тук</a>
                        </p>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@* Закоментирана секция за регистрация от трети страни
<div class="row justify-content-center mt-4">
    <div class="col-md-6 col-lg-4">
        <div class="card border-secondary">
            <div class="card-header text-center">
                <h5 class="card-title mb-0">Регистрация чрез друга услуга</h5>
            </div>
            <div class="card-body">
                <section>
                    <hr />
                    @{
                        if ((Model.ExternalLogins?.Count ?? 0) == 0)
                        {
                            <div class="text-center">
                                <p>
                                    Няма конфигурирани външни услуги за удостоверяване. Вижте тази <a href="https://go.microsoft.com/fwlink/?LinkID=532715">статия
                                    за настройване на това ASP.NET приложение за поддръжка на влизане чрез външни услуги</a>.
                                </p>
                            </div>
                        }
                        else
                        {
                            <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                                <div class="d-grid gap-2">
                                    @foreach (var provider in Model.ExternalLogins!)
                                    {
                                        <button type="submit" class="btn btn-outline-success" name="provider" value="@provider.Name" title="Регистрирайте се с вашия @provider.DisplayName акаунт">@provider.DisplayName</button>
                                    }
                                </div>
                            </form>
                        }
                    }
                </section>
            </div>
        </div>
    </div>
</div>
*@

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}